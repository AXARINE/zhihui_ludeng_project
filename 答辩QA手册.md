# 智慧路灯物联网项目 · 答辩 Q&A 手册

> 用途：小型答辩模拟提问。回答要**准确、简洁、有细节**——评委最反感背书，喜欢"我们遇到过、我们这样解决"的真实感。
> 提示：回答时多用"我们"、可适当带数据（如"设备 5 秒上报一次""首次下发约 20~35 秒"）。

---

## A. 项目整体（3 问）

**Q1：请用一句话介绍这个项目。**
> 基于 BearPi-HM Nano（Hi3861）+ E53_SC1 光照扩展板 + 华为云 IoTDA 的智慧路灯系统：设备端采集光照、按阈值自动联动开灯，云端通过 MQTT 接入 IoTDA，Rust 后端负责业务与权限，Web 管理端实现实时监测、远程控制、告警、智能问答，共覆盖 10 项功能。

**Q2：系统的整体架构是什么？（画图更好）**
> 四层：① 设备层：BearPi-HM Nano 跑鸿蒙 LiteOS，BH1750 每 50ms 采样、5 秒上报一次；② 云平台层：华为云 IoTDA（MQTT 1883 接入，设备影子、命令下发）；③ 业务层：Rust + axum 后端，轮询设备影子入库 PostgreSQL，北向 API 下发命令；④ 展示层：Web 管理端（原生 JS + ECharts，REST 轮询刷新）。

**Q3：你们实现了哪 10 项功能？（对应功能清单）**
> 实时光照监测、历史趋势可视化、光照联动（阈值自动开关灯）、远程手动控制、阈值参数设置、设备在线状态监控、设备离线告警、设备管理（增删/自动注册）、告警记录查询、维护智能问答，外加账号/角色/权限管理（RBAC）。

---

## B. 硬件与设备端（4 问）

**Q4：为什么选 BearPi-HM Nano / Hi3861？**
> 课程指定硬件；Hi3861 是 RISC-V 内核 WiFi SoC，资源小、低功耗，适合单传感器场景；配套鸿蒙 LiteOS 与 IoTDA SDK（oc_mqtt），上手快。

**Q5：光照传感器怎么工作的？**
> E53_SC1 板载 BH1750 数字光强传感器，走 I2C 接口（GPIO0/GPIO1），连续低分辨率模式 16ms 更新；固件 50ms 采样一次，换算系数乘 7（L 模式灵敏度补偿）。

**Q6：自动联动算法在设备端是怎么实现的？（防抖是重点）**
> 50ms 采样、滞回双阈值 + 连续 1 秒确认：灯灭时，光照连续低于"阈值-迟滞"才开灯；灯亮时，光照连续高于"阈值+迟滞"才关灯；灯亮时还要扣除灯光自照度（LAMP_SELF_LUX），否则补光灯照回传感器会形成 10Hz 自反馈振荡。这套逻辑避免了阈值附近的抖光。

**Q7：设备连不上云/掉线了怎么办？**
> 固件有 WiFi 重连（WifiConnectStatus 检测）和 MQTT 重连机制；云端靠心跳/上报判断离线并产生告警；后端定时轮询设备状态，页面实时展示在线/离线。实际开发中我们排查过：8883 TLS 在 Hi3861 的 iot_link 栈上不稳定，最终回退到 1883 明文 MQTT（组员实测结论）。

---

## C. 通信与云端（4 问）

**Q8：设备用什么协议连云端？数据格式？**
> MQTT（1883 明文，设备侧域名接入）；属性上报 JSON：服务 Light 下报 Luminance（光照）和 LightStatus（灯状态）；命令为 Light_Control_Led（参数 Led: ON/OFF/AUTO）。

**Q9：后端怎么和 IoTDA 通信？（北向）**
> HTTP REST，华为云 V5 接口，V11-HMAC-SHA256 衍生签名（HKDF 派生密钥 + SHA256 规范化请求），服务名 iotdm；后端用 reqwest 客户端，每 8 秒轮询设备影子获取最新光照，命令下发走 POST /devices/{id}/commands。

**Q10：遇到过最坑的通信问题是什么？**
> 两个：① 华为云下行接口首次调用要 20~35 秒（下行通道预热），后端原超时只有 10 秒导致"命令下发失败"误报——把超时调到 35 秒解决；② 固件误填了应用侧域名（iotda-app）连 8883，设备永远连不上——设备接入必须用 iotda-device 域名。

**Q11：为什么用轮询而不是推送/MQTT 订阅？**
> 课程规模下数据量小（单设备 5 秒一条），REST 轮询简单可靠、便于多端接入和调试；实时性上 10~20 秒刷新对路灯场景足够。若要更实时，可后续加 WebSocket 或 IoTDA 规则引擎转发。

---

## D. 后端与数据库（4 问）

**Q12：后端用什么技术栈？为什么？**
> Rust + axum（异步 Web 框架）+ sqlx（编译期校验 SQL）+ PostgreSQL。选 Rust 是因为性能好、内存安全、编译期就能发现很多问题；sqlx 的编译期 SQL 校验让手写 SQL 更安全。

**Q13：数据库表怎么设计的？**
> 主要表：device（设备信息/在线状态/灯态）、lux_record（光照历史）、config（阈值）、alarm（告警）、command_record（指令留痕）、app_user/role/permission/role_permission（RBAC 权限）、maintenance_knowledge（问答知识库）。用 sqlx migrate 管理迁移。

**Q14：设备数据是怎么进入数据库的？**
> 后端定时任务每 8 秒调 IoTDA 查询设备影子，把最新光照写入 lux_record，并更新设备在线状态；告警由后端根据离线/异常判定写入。

**Q15：如何保证接口稳定和可维护？**
> 统一错误类型（axum IntoResponse 映射状态码）、JWT 中间件 + 每接口权限校验、Swagger/OpenAPI 文档（/docs）、数据库迁移版本化、日志用 tracing（含请求耗时日志，便于排查慢接口）。

---

## E. 安全与权限（3 问）

**Q16：系统怎么做安全的？**
> ① 密码用 Argon2id 加盐哈希存储；② JWT 登录态（24h 有效期，密钥可用环境变量覆盖）；③ RBAC：每个接口校验权限码（如 device:manage、user:manage、role:manage），前端只是隐藏入口，真正的拦截在服务端；④ 华为云 AK/SK 只存后端 .env（gitignore 忽略），不进入前端和仓库。

**Q17：权限管理有什么特殊设计？**
> 三种角色：系统管理员（权限固定不可改）、路灯管理员（管设备/账号，不能改权限）、市政人员（只能监测/控制/看告警）。两个保护点：系统管理员角色的权限后端禁止修改（防止权限管理被锁死）；任何角色不能移除自己角色的"角色权限管理"权限。

**Q18：如果别人拿到你的 .env 会怎样？**
> 能拿到华为云 AK/SK 就是拿到了你账号的北向调用权，所以 .env 必须 gitignore、演示时不要泄露；这也是为什么密钥绝不能放前端（前端任何人打开页面都能看到）。

---

## F. 智能问答（2 问）

**Q19：智能问答是接了大模型吗？**
> 没有。是本地检索增强方案：关键词加权做意图识别（告警/光照/阈值/设备/指令/建议 6 类）→ 正则抽实体（设备、时间窗"最近 N 天"）→ 真查数据库（告警/光照统计/阈值/设备/指令）→ 结合维护知识库（maintenance_knowledge，6 类常见故障原因与建议）模板生成回答。离线可用、零成本、不泄露数据。如果后续要自由对话，可以换成接大模型 API，后端只需加一个转发。

**Q20：知识库内容怎么来的？**
> 根据项目常见故障场景预置：离线、光照异常、频繁开关（抖光）、通信超时、灯不亮、温度过高，每条含原因与处理建议；回答"路灯频繁开关怎么办？"会命中"阈值设置不合理→增大滞回区间"。

---

## G. 工程挑战与亮点（2 问）

**Q21：项目里最难的/最有成就感的点？**
> 三个：① 全链路打通（设备→IoTDA→后端→数据库→前端）；② 定位"命令下发失败"根因（不是网络/证书，是华为云下行接口首次响应慢 + 超时设置不合理），用请求耗时日志逐层排查；③ RBAC 权限体系 + 防权限锁死设计（系统管理员权限不可改），是从真实系统管理场景思考出来的。

**Q22：你们团队怎么协作的？**
> GitHub 仓库统一管理，分模块开发（设备端/云侧/后端/前端）；踩坑经验（1883 回退、域名区分、超时调整）沉淀进 README 和代码注释，方便其他人复用。

---

## H. 改进方向（2 问）

**Q23：有哪些不足和后续改进？**
> ① 数据刷新目前是轮询，可换 WebSocket 或 MQTT 订阅做到秒级实时；② 单机部署，可容器化上云（Docker Compose 已有基础）；③ 智能问答是模板化的，可接大模型升级自由对话；④ 设备认证可升级到 X.509 证书（MQTTS）更安全；⑤ 可加多区域路灯分组、能耗统计、定时策略等。

**Q24：这个系统离真实商用还差什么？**
> 设备量级上需要水平扩展（消息队列、分库分表）；运维上要监控告警、日志聚合、灰度发布；安全上要证书设备认证、密钥托管（KMS）；业务上要计费、多租户、SLA 保障。目前是课程规模的最简可行闭环。
